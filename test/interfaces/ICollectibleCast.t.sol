// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.30;

import {Test} from "forge-std/Test.sol";
import {ICollectibleCast} from "../../src/interfaces/ICollectibleCast.sol";

contract ICollectibleCastTest is Test {
    function test_Interface_CanBeImported() public pure {
        // This test verifies the interface exists and can be imported
        // We'll test actual implementation when we build the contract
    }
}